import type { App } from 'vue'
import 'vue-sonner/style.css'

/**
 * Глобальные настройки уведомлений.
 * Сейчас достаточно просто подключить стили,
 * но при необходимости можно регистрировать плагины.
 */
export function installToast(_app: App) {
  // пока что дополнительных настроек нет
}
